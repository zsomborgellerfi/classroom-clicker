# Optional root-level overrides.
# docker-compose already reads backend/.env, so you only need this file if
# you want to override values for local scripts or container env vars.
#
# Example:
# JWT_SECRET=super_secret_value
